import styled from 'styled-components';

const Wrapper = styled.div``;

interface IBubble{
  text: string;
  color: string;
}

export default function Bubble({}) {
  return (
    <Wrapper></Wrapper>
  )
}
